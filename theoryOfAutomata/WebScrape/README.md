# System Requirements
```
Make sure to have Beautiful Soup 4 installed
using python 3.* 
In code where it says *.html, you can change the file name to 1 of 3 input file names.
when you hit run,
it will scrape the needed information from the HTML code,
 and output to the corresponding .txt file.
```
